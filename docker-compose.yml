version: "3"
    
volumes:
  volume-postgres:
    
services:
  db:
    image: postgres:12-alpine
    ports:
      - 5432:5432
    volumes:
      - volume-postgres:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: jsninjify
      POSTGRES_PASSWORD: ThisBePassword
      POSTGRES_DB: jsninjify
    
  ninjify-api:
    build: .
    container_name: ninjify-API
    depends_on:
      - db
    links:
      - db
    ports:
      - "5000:5000"
    environment:
      - DB_HOST=db
      - DB_USER=jsninjify
      - DB_PASSWORD=ThisBePassword
      - DB_PORT=5432
      - DB_NAME=jsninjify
      - FLASK_DEBUG=1
      - FLASK_APP=jsninjify